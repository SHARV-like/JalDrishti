# Satellite assets

Store only prepared, traceable, presentation-ready derivatives here. Keep raw raster downloads out of Git. For every asset, record the actual source, date window, cloud/quality information, band mapping, processing method, checksum or identifier, and `data_status`.
