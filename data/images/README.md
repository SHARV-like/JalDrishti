# Demo field images

Place only approved, non-sensitive, demonstration field images here. Strip unnecessary EXIF before committing; retain only documented metadata required for a controlled demo. Large/raw images are excluded by `.gitignore` when placed in designated raw-data directories.
